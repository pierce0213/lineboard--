# 更新日志 Changelog

本项目的全部值得记录的变更都写在这里。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-20

首个正式版。一个 HTML 文件，双击就能用的对称辅助绘图板。

### 新增 Added

**绘图核心**

- 9 种绘制工具：直线 / 折线 / 箭头 / 矩形 / 椭圆 / 弧线 / 文本 / 橡皮 / 填格，快捷键 `1`–`9`
- 辅助对称：左右 / 上下镜像、四象限、中心对称 180°、径向 N 等分（2–24 份），
  对称轴心可直接拖动；对称副本在渲染期实时派生，不写入数据，关掉即干净消失
- 等轴测网格：30° 三向立体格，斜棱偏移 = 纵向偏移 × 1.732，专为画正方体等立体图形设计
- 按格填色：悬停高亮整格、单击填充、拖动连刷；等轴测下填的是菱形面片，天然带体块感
- 双重吸附：网格点（蓝点）与已有线条端点 / 交点（红圈），接缝严丝合缝
- 弧线工具：拖出弦、移动定弧度、单击落笔；落笔即换算为普通折线，撤销 / 吸附 / 橡皮 / 导出 / AI 全链路零改动兼容
- 2 维坐标系：坐标轴 + 边缘标尺 + 原点 O + 光标实时坐标，y 轴向上 / 向下可切换

**AI 生成（可选增强）**

- 一句话（可附参考图）→ 大模型输出结构化绘图指令 JSON → 直接画成**可继续编辑**的线条图形，
  而不是一张不能改的图片；生成结果可橡皮删除、可整批撤销、可再开对称
- 内置 7 家预设：DeepSeek、通义千问 · 百炼、智谱 GLM、硅基流动、月之暗面 Kimi、OpenAI、Anthropic Claude，
  也支持任意 OpenAI / Anthropic 兼容的中转地址
- 三档精细度（简洁 / 标准 / 精细）控制画面复杂度；推理类模型（deepseek-reasoner 等）
  自动放大输出上限并对截断答案做 salvage 容错
- 隐私：API Key 只存你自己浏览器的 localStorage，与作品数据分属两个独立存储项，
  请求由浏览器直连你填写的接口，导出的 PNG / 存档 / 仓库都不含密钥

**图片转线条 / 转填格（纯本机，不需要密钥）**

- 图片 → 线条：灰度 + Sobel 边缘，本机完成
- 图片 → 像素填格：四档精度 **标准 52 / 高清 80 / 超清 110 / 原色 130 格**；
  众数采样（而非平均色）+ 2× 子格四票多数决，边缘不出脏混色；
  自适应聚色并色（96 色上限），浅色主体（银发、白衣）与柔和相邻色可保；
  四周空白自动裁掉、背景色洪泛剔除（带渗透预算，防止柔和渐变主体被啃穿）、
  长图保纵横比不压扁，上限 16000 格

**文档与导出**

- 每 4 秒自动保存到浏览器本地，刷新 / 误关自动恢复；大文档自动拉长间隔避免卡顿
- 作品可导出 `.json` 存档（v2 格式：色块按「样式 + 格尺寸」压缩成组，
  4300 格从约 700KB 降到 37KB，旧版 v1 存档仍可读），导出 2 倍分辨率白底 PNG

**跨平台**

- Windows / macOS / Linux / Android / iPadOS / `file://` 双击打开，全部验证可用
- macOS 快捷键文案自动显示 `⌘`；非 US 键盘布局按物理数字键同样能切工具；
- 手机竖屏面板自动收成图标栏（画布占视口 86%，此前只剩 34%）；
  多指同时落笔不串笔、系统手势打断不卡笔（`isPrimary` 校验 + `pointercancel` 兜底）
- Linux 中文字体回退（Noto Sans CJK / 文泉驿）与 Firefox 控件样式（滑块 / 滚动条 / 复选框）齐备

### 兼容性 Compatibility

- 存档格式 v2 向下兼容 v1，旧存档无需转换
- 测试基线：149 项功能回归（8 套）+ 12 平台场景 × 2 内核（Chrome / Edge）各 278 项断言全绿

### 安全 Security

- 静态扫描确认源码无任何内置密钥；`.gitignore` 兜底排除 `.env` / `*.key` / `*.pem` 等

[1.0.0]: https://github.com/pierce0213/lineboard--/releases/tag/v1.0.0
